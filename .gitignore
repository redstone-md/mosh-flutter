# Node
node_modules/
npm-debug.log*

# Vite and Tauri frontend builds
dist/
.vite/

# Rust and Tauri native builds
target/
src-tauri/target/

# Local native runtime artifacts
moss-runtime/
src-tauri/moss-runtime/*
!src-tauri/moss-runtime/
!src-tauri/moss-runtime/.gitkeep
moss.dll
moss.h
libmoss.so
libmoss.dylib
libmoss.h

# Local environment and editor state
.env
.env.*
!.env.example
.DS_Store
Thumbs.db

# Agent scratch space
.codex/*
!.codex/.gitkeep
.continuum/
.aislop/
docs/UX_UI_FIX_PLAN.md
# Merged run artifact from scripts/probe-e2e.mjs — regenerated every run.
probe-timeline.jsonl



# Flutter / Dart / Rust build artifacts (added by Flutter scaffold)
build/
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
/mosh-core/target/
/target/
.pub-cache/
.pub/
ios/Pods/
macos/Pods/
generated_plugin_registrant.dart
# pubspec.lock for Flutter; keep package-lock.json (npm)

# Flutter gen-l10n generated files (regenerated by flutter gen-l10n / flutter pub get)
lib/l10n/app_localizations.dart
lib/l10n/app_localizations_*.dart
# transient PowerShell commit-message scratch file
commit_msg.txt
.commit_msg.tmp
cm.txt
# transient flutter analyze stdout dump (do not commit)
analyze.out
